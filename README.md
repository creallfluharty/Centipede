# make sure you have (SFML)[https://www.sfml-dev.org] installed
# ie. on debian ```sudo apt-get install libsfml-dev```
#     on arch   ```pacman -S sfml```
# compile with ```make all```
# Forked from MCassimus/Centipede

